require('zen-mode').setup {
  window = {
    -- shade backdrop of the Zen window
    backdrop = 1,
    -- width in number of columns
    width = 80,
    options = {
      -- disable column number
      number = false,
    },
  },
}
